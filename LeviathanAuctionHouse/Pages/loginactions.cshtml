@page
@model LeviathanAuctionHouse.Pages.loginactionsModel
@using MySql.Data.MySqlClient;
@using System.Data;
@using Newtonsoft.Json;
@{
}

@{
    Layout = null;
    //Request.Query : รับinput จาก Client แบบ GET
    var action = Request.Form["action"];
    var result = "";
    if (action == "login")
    {

        var username = Request.Form["uname"];
        var password = Request.Form["psw"];

        //แก้การตรวจสอบ if เป็นการ select จากฐานข้อมูล
        if (username == "admin1" && password == "123")
        {
            result = "Login success";
            HttpContext.Session.SetString("username", username);
            HttpContext.Session.SetString("displayname", "Admin No.1");
        }
        else if (username == "admin2" && password == "123")
        {
            result = "Login success";
            HttpContext.Session.SetString("username", username);
            HttpContext.Session.SetString("displayname", "Admin2nd place");
        }
        else 
        {
            result = "Login Failed";
        }

    }


}
@Html.Raw(result)
